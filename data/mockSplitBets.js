"use strict";

module.exports = [
  {
    splitbet_id: 11111111,
    sender_id: 'number', // references 'users(guid)'
    recipient_id: 'number', // references 'users(guid)'
    card_id: 'number',
  }
]
